CREATE DATABASE HELLO26;

USE HELLO26;

CREATE TABLE EMPDETAILS(
	ID INT PRIMARY KEY,
    NAME VARCHAR(30),
    SALARY INT
);
INSERT INTO EMPDETAILS VALUES (1,"JOHN",40000),(2,"ALEX",25000),(3,"SIMON",43000);
CREATE TABLE STATUS(
	ID INT PRIMARY KEY,
    NAME VARCHAR(30),
    STATUS VARCHAR(10)
);
INSERT INTO STATUS VALUES (1,"JOHN","MARIIRED"),(3,"SIMON","MARRIED"),(4,"STELLA","UNMARRIED");

SELECT * FROM EMPDETAILS;
SELECT * FROM STATUS;

SELECT EMPDETAILS.ID,STATUS.NAME,SALARY,STATUS
FROM EMPDETAILS
INNER JOIN
STATUS
ON EMPDETAILS.ID = STATUS.ID;

SELECT EMPDETAILS.ID,STATUS.NAME,SALARY,STATUS
FROM EMPDETAILS
JOIN
STATUS
WHERE EMPDETAILS.ID = STATUS.ID;

SELECT EMPDETAILS.ID,STATUS.NAME,SALARY,STATUS
FROM EMPDETAILS
INNER JOIN
STATUS
WHERE EMPDETAILS.ID = STATUS.ID;


SELECT EMPDETAILS.ID,EMPDETAILS.NAME,SALARY,STATUS
FROM EMPDETAILS
LEFT JOIN
STATUS
ON EMPDETAILS.ID = STATUS.ID;

SELECT EMPDETAILS.ID,EMPDETAILS.NAME,SALARY,STATUS
FROM EMPDETAILS
LEFT JOIN
STATUS
ON EMPDETAILS.ID = STATUS.ID;

CREATE TABLE EMPLOYEES(
	EMP_ID INT PRIMARY KEY,
    NAME VARCHAR(20),
    DEPARTMENT_ID INT,
    SALARY INT
);
INSERT INTO employees (emp_id, name, department_id, salary) VALUES
(101, 'Alice', 1, 50000),
(102, 'Bob', 2, 60000),
(103, 'Charlie', 2, 70000),
(104, 'David', 3, 45000),
(105, 'Eve', NULL, 40000),
(106, 'Frank', 4, 55000),
(107, 'Grace', 2, 62000);

SELECT * FROM EMPLOYEES;


CREATE TABLE DEPARTMENTS(
	DEPARTMENT_ID INT PRIMARY KEY,
    DEPARTMENT_NAME VARCHAR(20)
);
INSERT INTO departments (department_id, department_name) VALUES
(1, 'HR'),
(2, 'Engineering'),
(3, 'Marketing'),
(4, 'Finance');

SELECT * FROM DEPARTMENTS; 

CREATE TABLE PROJECTS(
	PROJECT_ID INT PRIMARY KEY,
    PROJECT_NAME VARCHAR(20),
    EMP_ID INT
);
INSERT INTO projects (project_id, project_name, emp_id) VALUES
(201, 'Website Redesign', 102),
(202, 'Mobile App', 103),
(203, 'Ad Campaign', 104),
(204, 'Data Migration', 106),
(205, 'Backend Optimization', 107),
(206, 'Product Launch', NULL); 

SELECT * FROM PROJECTS;


SELECT NAME,DEPARTMENT_NAME
FROM EMPLOYEES
INNER JOIN
DEPARTMENTS
ON EMPLOYEES.DEPARTMENT_ID = DEPARTMENTS.DEPARTMENT_ID;

SELECT EMPLOYEES.EMP_ID,NAME
FROM EMPLOYEES
JOIN
PROJECTS
ON EMPLOYEES.EMP_ID = PROJECTS.EMP_ID;

SELECT EMPLOYEES.NAME,PROJECTS.PROJECT_NAME
FROM EMPLOYEES
INNER JOIN
PROJECTS
ON EMPLOYEES.EMP_ID = PROJECTS.EMP_ID;

SELECT NAME,DEPARTMENT_NAME
FROM EMPLOYEES
LEFT JOIN
DEPARTMENTS
ON EMPLOYEES.DEPARTMENT_ID = DEPARTMENTS.DEPARTMENT_ID;

SELECT EMPLOYEES.NAME,PROJECTS.PROJECT_NAME
FROM EMPLOYEES
LEFT JOIN
PROJECTS
ON EMPLOYEES.EMP_ID = PROJECTS.EMP_ID;